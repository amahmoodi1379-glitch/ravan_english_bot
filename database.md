# قوانین دیتابیس و Migration (مهم — همیشه رعایت شود)

این پروژه روی **Cloudflare D1** (SQLite) اجرا می‌شود و یک دیتابیس **live با داده‌های واقعی**
(واژگان، سوالات، متن‌های ریدینگ، کاربران) دارد. این داده‌ها ارزشمندند و نباید از بین بروند.

## منبع حقیقت (Single Source of Truth)

- فایل `migrations/schema.sql` **وضعیت کامل و فعلی** کل دیتابیس را نشان می‌دهد.
- **قبل از هر کاری روی دیتابیس، اول `migrations/schema.sql` را بخوان** تا ساختار را بفهمی.
- پوشه `migrations/archive/` فقط تاریخچه است؛ برای فهمیدن دیتابیس به آن نیاز نداری و نباید آن را تغییر بدهی.

## وقتی اسکیما را تغییر می‌دهی (الزامی)

هر تغییر در ساختار دیتابیس (افزودن/تغییر جدول، ستون، ایندکس) باید **هم‌زمان** این دو کار را انجام دهد:

1. یک فایل migration جدید با شماره ترتیبی بساز: `migrations/NNNN_description.sql`
   (مثلاً `migrations/0026_add_xyz.sql`). شماره باید از آخرین migration بزرگ‌تر باشد.
2. `migrations/schema.sql` را طوری به‌روزرسانی کن که دقیقاً با نتیجه‌ی نهایی بخواند.

> این دو فایل **هرگز** نباید با هم در تناقض باشند. اگر فقط یکی را عوض کنی، اشتباه است.

- migration های قدیمی که قبلاً اجرا شده‌اند **immutable** هستند؛ هرگز محتوای آن‌ها را ویرایش نکن.

## نحوه اجرا (مهم)

- migration ها **به‌صورت خودکار هنگام deploy اجرا نمی‌شوند**. کاربر باید آن‌ها را
  **دستی در داشبورد Cloudflare D1 → Console** اجرا کند.
- بنابراین هر وقت یک migration جدید ساختی، **صریحاً به کاربر بگو** که باید آن فایل را
  در D1 اجرا کند، و دقیقاً کدام فایل را.

## ایمنی داده‌ها

- دستورات مخرب (`DROP TABLE`, `DELETE`, `TRUNCATE`, `DROP COLUMN`) را **بدون تأیید صریح کاربر اجرا نکن**.
- migration ها را تا حد امکان idempotent بنویس: `CREATE TABLE IF NOT EXISTS`,
  `CREATE INDEX IF NOT EXISTS`. برای افزودن ستون از `ALTER TABLE ... ADD COLUMN` استفاده کن.
- توجه: D1 ممکن است چند `ALTER TABLE` در یک batch را پشتیبانی نکند؛ در صورت لزوم به کاربر بگو
  هر `ALTER` را جداگانه اجرا کند.

## نکات خاص این پروژه

- جدول `user_words_sm2` با وجود نامش، **وضعیت FSRS** را نگه می‌دارد (نه SM2). این نام به‌خاطر
  سازگاری با گذشته است؛ **آن را تغییر نده / rename نکن**.
- الگوریتم مرور فاصله‌دار = **FSRS-5** در `src/utils/fsrs.ts`. ستون‌های مرتبط:
  `stability`, `difficulty`, `card_state`, `lapses`, `reps`, `next_review_date`.
- واژه‌های «سخت» (leech) = واژه‌هایی با `lapses >= LEITNER_LEECH_THRESHOLD` (در `constants.ts`).
